"""Hardcoded Apify actor registry (phase 1).

Holds the curated `apify_actors.json` file shipped with the package. The loader
that reads and filters it lives in a later prompt; this package is just the home
for the data file and its marker.
"""
